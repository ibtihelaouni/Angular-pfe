import { Routes } from '@angular/router';
import { ListComponent } from 'app/modules/admin/company/list/list.component';

export default [
    {
        path     : 'list',
        component: ListComponent,
    },

] as Routes;
